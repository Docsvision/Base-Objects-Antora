= Исправленные ошибки

include::webclient:admin:partial$table.adoc[]
|ERR-2829
|При выборе группы в _Справочнике сотрудников_ происходил поиск сотрудников во всех вложенных группах, что замедляло работу.

|ERR-2814 (SUP-6899)
|В документации модуля отсутствовал раздел с особенностями разворачивания резервных копий БД с сателлитами.

|ERR-2773 (SUP-6849)
|Пример кода блокировки карточки в руководстве разработчика был составлен с ошибками.

|ERR-2760 (SUP-6830)
|При подключении с использованием SQL-аутентификации в журнал FileService передаются логин и пароль.

|ERR-2747 (SUP-6821)
|Во время подписания появлялось пустое окно с ошибкой, если адрес OCSP сервиса в сертификате и в справочнике системных настроек не совпадали.

|ERR-2735 (SUP-6806)
|В руководстве пользователя модуля отсутствовала информация о том, что при экспорте файлов из карточки экспортируются только действительные подписи текущих версий файлов.

|ERR-2724 (SUP-6542)
|При открытии карточки в {wc}е возникала ошибка `Internal exception, error code: InvalidRow`.

|ERR-2723 (SUP-6787)
|Исправлена ошибка, при которой пользователю было доступно редактирование истории карточки в столбце _Описание события_.

|ERR-2703 (SUP-6784)
|В документации отсутствовала информация о необходимости тарифа с оплатой доступа к API для подключения к Диадок.

|ERR-2698 (SUP-6740)
|Возникала ошибка `Непредусмотренный конец файла` в пользовательских БП из-за параллельной генерации схем для одного и того же типа карточек в разных потоках.

|ERR-2683 (SUP-6638)
|В документации отсутствовала информация о необходимости перезапускать пул IIS после выполнения джоба `purge_deleted_object_info`.

|ERR-2624 (SUP-6738)
|При подключении к {mssql} 2019 из утилиты _CardManager_ появлялась ошибка `Current {dv} Server edition doesn't support Microsoft SQL Server`.

|ERR-2575 (SUP-6693)
|Не срабатывал БП подписки на события в карточке через {wc}.

|ERR-2520 (SUP-6669)
|На скриншотах конструктора справочников в документации был отображён неактуальный набор кнопок.

|ERR-2509 (SUP-6640)
|Исправлена ошибка, когда не работало предварительное кэширование разметок корневого вида.

|ERR-2505 (SUP-6624)
|исправлена ошибка, из-за которой Elasticsearch не индексировал поля, ссылающиеся на справочники.

|ERR-2498 (SUP-6651)
|При удалении файла из карточки документа возникала ошибка `Not Found`.

|ERR-2470 (SUP-6593)
|Информация по работе БП "Процесс актуализации состояния сотрудников" при замещении, представленная в документации, была недостаточно полной.

|ERR-2415 (SUP-6565)
|При работе системы в журнале Storage Server записывалась ошибка: `StorageServerRuntime Error: 0 : Не удалось найти часть пути`.

|ERR-2325 (SUP-6495)
|В документации отсутствовала информация о работе замещения для случаев, когда указано несколько заместителей.

|ERR-2271 (SUP-6453)
|При выборе метода для события в обработчике из задания На согласование в код добавлялся пустой метод. При этом методы по другому событию удалялись.

|ERR-2174 (SUP-6390)
|Календарь по умолчанию не учитывался при расчёте сроков в заданиях.

|ERR-2170 (SUP-6394)
|При выборе значения из конструктора справочников в представлении не отображалась информация.

|ERR-1914 (SUP-6260)
|Исправлена ошибка, при которой метку в элементе управления `_Да / Нет_` было невозможно скрыть.

|ERR-1819 (SUP-6182)
|После обновления модуля {bo} появлялась ошибка `Value cannot be null. parameter name: description` при открытии заданий.

|ERR-1798 (SUP-6164)
|При просмотре определённых свойств в структуре разметок, {wincl} или РМА переставали отвечать.

|ERR-1797 (SUP-6143)
|При создании БД возникала ошибка `System.Reflection.ReflectionTypeLoadException: Не удается загрузить один или более запрошенных типов. Обратитесь к свойству LoaderExceptions для получения дополнительных сведений`.

|ERR-1790 (SUP-6782)
|При открытии свойств элемента управления `_Кнопка_`, добавленного в разметку возникала ошибка.

|ERR-1685 (SUP-6082)
|Исправлена ошибка, при которой в журнал {wincl}а записывалось: `Error occurred when calling Cryptography service with address`.

|ERR-1677 (SUP-6037)
|Исправлена ошибка, вызывавшая долгое открытие карточек заданий.

|ERR-1668 (SUP-5970)
|Было возможно завершить задание на подписание с использованием УКЭП без ошибки, если просрочена лицензия CSP.

|ERR-1583 (SUP-6035)
|Во время поиска сотрудника при создании или делегировании группы заданий возникала задержка.

|ERR-1514 (SUP-5984)
|В файловом хранилище оставались удалённые из карточки файлы, если файлы были удалены из карточки до её сохранения.

|ERR-1485 (SUP-5955)
|Текст ошибки `Делегирование вручную запрещено` при невозможности делегирования был неинформативен и был заменён на `Повторное делегирование запрещено`.

|ERR-1477 (SUP-5934)
|При использовании БД {pgsql} в историю сообщений карточки записывались пустые сообщения.

|ERR-1396 (SUP-5907)
|Для хранения подключения к базе журналов использовалась устаревшая настройка `LogDatabaseConnectionString`.

|ERR-1280 (SUP-5798)
|Исправлена ошибка, при которой если у группы заданий нет собственного списка заданий (TaskList), нарушалась логика вычисления подчиненных заданий и групп заданий.

|ERR-1279 (SUP-5819)
|При работе с согласованием через почтовый клиент, в "Ходе согласования" не отображались результаты делегирования.

|ERR-1233 (SUP-5762)
|Возникала ошибка `An item with the same key has already been added` при просмотре и последующем закрытии элемента управления `_Назначенные исполнители_`.

|ERR-1185 (SUP-5747)
|При обновлении БД возникала ошибка `The operation failed because an index or statistics with name 'dvsys_log_idx_date' already exists on table 'dbo.dvsys_log'`.

|ERR-1179 (SUP-5707)
|Расширенный отчёт dvreport_get_data_\{3d695ba8-2815-4785-b251-9c551ddbd3db} осуществлял полный просмотр вместо поиска по индексу.

|ERR-1166 (SUP-5746)
|В документации не были указаны ограничения количества символов для полей типа `Decimal`.

|ERR-1154 (SUP-5721)
|В руководстве по обновлению системы с версии 5.5 до 5.5.4 отсутствовало уточнение о необходимости заменить вручную `dvad_Operand_Reconcile_TasktreeEmployees`.

|ERR-1134 (SUP-5725)
|Исправлена ошибка в системном сообщении "Сервер не ответил в течение заданного промежутка времени".

|ERR-1114 (SUP-5701)
|Исправлена ошибка, при которой отсутствовали кнопки и задний фон в письме почтового клиента.

|ERR-1064 (SUP-5641)
|В документации разработчика не был описан метод `IStaffService.GetEmployeeManager`.

|ERR-1058 (SUP-5680)
|При разворачивании узла конструктора справочников область со списком узлов прокручивалась до начала.

|ERR-1019 (SUP-5626)
|При получении пустого скрипта, возникала ошибка, из-за чего не происходила привязка скриптов.

|ERR-1012 (SUP-5638)
|Не работало вытеснение файлов в хранилище filestream после ограничения прав пользователя.

|ERR-1006 (SUP-5646)
|Флаг `*Изменение на уровне БД*` можно было установить для параметрических поисковых запросов.

|ERR-999 (SUP-5614)
|При использовании {dv} 5.4 с накопительным обновлением 9 у пользователей не отображались способы создания для видов, находящихся на том же уровне в справочнике видов.

|ERR-956 (SUP-5611)
|Исправлена ошибка, снижавшая скорость работы из-за большого количества запросов вида `401 Unauthorized, NTLMSSP_CHALLENGE` от {wc}а к Storage Server.

|ERR-943 (SUP-5526)
|Файл, вытесненный в файловую систему не возвращался в БД из очереди.

|ERR-922 (SUP-5335)
|При работе служб с кэш-сервером Redis в журнал записывались ошибки вида `Cache error`.

|ERR-894 (SUP-5558)
|Возникала ошибка `Unable to initialize security context extension` при использовании sql-скрипта в бизнес-процессе.

|ERR-886 (SUP-5526)
|Исправлена ошибка, при которой файлы не вытеснялись во внешнее хранилище.

|ERR-882 (SUP-5541)
|В письме почтового клиента текст содержания отображался с тегами, если в содержании задания был указан любой текст.

|ERR-871 (SUP-5536)
|Исправлена ошибка, при которой не применялись заданные локализованные названия для папок в дереве папок {wincl}а.

|ERR-844 (SUP-5514)
|При открытии файла журнала системы в утилите Log Viewer возникала ошибка.

|ERR-842 (SUP-5502)
|При сериализации поля `Decimal` в XML происходила ошибка `System.NotSupportedException: Don't know how to safe convert value '9000,00000000000000' of type Decimal to string` из-за которой выгружались не все секции карточки при использовании REST Object Manager.

|ERR-785 (SUP-5457)
|Исправлена ошибка, когда запрещённая по ролевой операция _Удалить_ не позволяла удалить карточку версии файла.

|ERR-474 (SUP-2051)
|Исправлена ошибка, когда невозможно было задать период автоматического обновления представления папки более 999.

|ERR-472 (SUP-2570)
|В почтовом клиенте при положительной семантике завершения задания на согласование, текст письма копировался в отчет по заданию.

|ERR-471 (SUP-3299)
|При согласовании из почты не передавались комментарии в {wc}.

|ERR-470 (SUP-3875)
|Не открывалась ссылка на группу заданий в уведомлении автора о завершении группы заданий.

|ERR-464 (SUP-4203)
|При агрегации значений для представления символ пробела отображался как `\&#x20;`.

|ERR-463 (SUP-4274)
|После сканирования был виден весь лист, но изображение получалось уменьшенным и присутствовала белая рамка по всем краям.

|ERR-417 (SUP-5104)
|Возникала ошибка при смене состояния карточки документа с помощью скрипта: `Unable to set properties to signed state`.

|ERR-356 (SUP-5434)
|При использовании маршрутизации *_Письмо с описанием задания_* применялся тот же вид письма, что и при маршрутизации *_Задача почтового клиента_*.
|===
